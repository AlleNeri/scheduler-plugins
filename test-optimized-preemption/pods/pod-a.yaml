apiVersion: apps/v1
kind: Deployment
metadata:
  name: pod-a-deployment
  labels:
    app: pod-a
spec:
  replicas: 1
  selector:
    matchLabels:
      pod: pod-a
  template:
    metadata:
      labels:
        pod: pod-a
    spec:
      containers:
        - name: stress-box
          image: alpine:latest
          stdin: true
          tty: true
          resources:
            requests:
              cpu: "1000m"
              memory: "1800Mi"
            limits:
              cpu: "1000m"
              memory: "1800Mi"
